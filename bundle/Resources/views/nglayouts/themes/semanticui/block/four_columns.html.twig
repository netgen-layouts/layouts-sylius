{% extends '@nglayouts/block/block.html.twig' %}

{% block content %}
    <div class="ui twelve column stackable grid">
        <div class="three wide column">
            {{ nglayouts_render_placeholder(block, 'left') }}
        </div>
        <div class="three wide column">
            {{ nglayouts_render_placeholder(block, 'center_left') }}
        </div>
        <div class="three wide column">
            {{ nglayouts_render_placeholder(block, 'center_right') }}
        </div>
        <div class="three wide column">
            {{ nglayouts_render_placeholder(block, 'right') }}
        </div>
    </div>
{% endblock %}
