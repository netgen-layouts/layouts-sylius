{% set product = item.object %}

{% if product.image is not null %}
    <div class="image">
        <img src="{{ product.image.path|imagine_filter('ngbm_sylius_product_app_preview') }}" />
    </div>
{% endif %}

<div class="name">
    <p>{{ item.name }}</p>
</div>
